---
import { Image } from "astro:assets";
// Importamos la imagen desde la nueva ruta
import profileImg from "../assets/img/profile.webp";
---

<section
  id="about"
  class="mx-auto flex w-full max-w-5xl scroll-mt-20 flex-col space-y-12 p-4 py-12 md:py-16 lg:py-20"
>
  <h2
    class="reveal text-balance text-center text-3xl font-bold tracking-tight text-slate-900 dark:text-white md:text-5xl"
  >
    Sobre mí
  </h2>

  <div
    class="flex flex-col items-center justify-center gap-10 md:flex-row md:gap-16"
  >
    <div class="reveal-left order-1 flex-shrink-0 md:order-2">
      <div class="group relative">
        <div
          class="absolute -inset-1 rounded-full bg-gradient-to-r from-blue-600 to-cyan-400 opacity-25 blur transition duration-1000 group-hover:opacity-50"
        >
        </div>
        <Image
          src={profileImg}
          alt="Foto de perfil de José Rodríguez"
          width={250}
          height={250}
          loading="lazy"
          class="relative size-48 rounded-full border-2 border-slate-200 object-cover shadow-xl dark:border-slate-800 md:size-56"
        />
      </div>
    </div>

    <div
      class="reveal-right order-2 max-w-2xl space-y-6 text-justify text-lg leading-relaxed text-slate-800 dark:text-slate-200 md:order-1"
    >
      <p>
        Soy <span class="font-semibold text-blue-700 dark:text-blue-400"
          >Ingeniero en Sistemas Computacionales</span
        >
        egresado del <span class="font-medium text-slate-900 dark:text-white"
          >TecNM Campus de Los Ríos</span
        >, con especialidad en
        <span class="font-semibold text-blue-700 dark:text-blue-400"
          >Ingeniería de Software</span
        >.
      </p>

      <p>
        Actualmente me desempeño como <span
          class="font-medium text-slate-900 dark:text-white"
          >desarrollador frontend freelance</span
        > donde transformo ideas en productos digitales funcionales y modernos. Mi
        prioridad es siempre entregar soluciones con alto rendimiento y un código
        limpio y mantenible.
      </p>

      <p>
        Mi enfoque se centra en aplicar las <span
          class="font-medium text-slate-900 dark:text-white"
          >mejores prácticas</span
        >
        de la industria, asegurando que cada proyecto no solo sea visualmente impactante,
        sino también escalable y eficiente a largo plazo.
      </p>
    </div>
  </div>
</section>

<style>
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s ease-out;
  }
  .reveal-left {
    opacity: 0;
    transform: translateX(50px);
    transition: all 0.8s ease-out;
  }
  .reveal-right {
    opacity: 0;
    transform: translateX(-50px);
    transition: all 0.8s ease-out;
  }
  .show {
    opacity: 1;
    transform: translate(0);
  }
</style>

<script>
  const observerOptions = {
    root: null,
    rootMargin: "0px",
    threshold: 0.15,
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("show");
      }
    });
  }, observerOptions);

  document
    .querySelectorAll(".reveal, .reveal-left, .reveal-right")
    .forEach((el) => observer.observe(el));
</script>
